const navLinks = ["Home", "News", "Matches", "Transfers", "Opinion", "Fan Zone", "Club"];

// Update link handling for "News"
const route = (link) => {
    switch (link) {
        case "News":
            return "/news";
        // other cases
    }
};