version: 1
backend:
  phases:
    build:
      commands:
        - npm ci
        - npx prisma migrate deploy
frontend:
  phases:
    build:
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*